#include <list>
#include "Event.h"

class Input {

	//std::list<Event> Keyboard;
	//friend void BindKeyboard(SDL_Keysym keysym, Uint32 type = SDL_KEYDOWN, Uint8 state = SDL_PRESSED, bool once = false);
	public:
		void Run();
};
